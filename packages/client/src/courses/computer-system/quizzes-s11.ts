import type { Quiz } from '@learncourse/framework/types';

// === 性能优化 ===

// ── 选择题 ──
export const S11_QUIZZES: Quiz[] = [
  {
    id: "q-s11-3",
    moduleId: "s11",
    question: "某程序总执行时间中，可优化部分占比 f = 60%。若将该部分加速至原来的 4 倍（即 k = 4），根据 Amdahl 定律 S = 1 / [(1-f) + f/k]，整个程序的加速比 S 最接近下列哪个值？",
    options: [
      { text: "A. 4.00", isCorrect: false },
      { text: "B. 2.50", isCorrect: false },
      { text: "C. 1.82", isCorrect: true },
      { text: "D. 0.55", isCorrect: false },
    ],
    feedbackCorrect: "正确！S = 1 / [(1-0.6) + 0.6/4] = 1 / [0.4 + 0.15] = 1 / 0.55 ≈ 1.82。即使优化部分加速4倍，整体加速比也不到2倍——这正是Amdahl定律的核心洞见：不可优化部分（串行瓶颈）严重制约整体加速比的上限。当k→∞时S的上限仅为1/(1-f)=2.5，优化空间被串行部分锁死。",
    feedbackWrong: "不对。S = 1 / [(1-0.6) + 0.6/4] = 1 / [0.4 + 0.15] = 1 / 0.55 ≈ 1.82。逐项分析：A=4.00是错误地把整体加速比等同于部分加速比k，忽略了40%不可优化部分的拖累；B=2.50是计算了S的上限1/(1-f)=1/0.4=2.5（即k→∞的理想情况），而非k=4时的实际值；D=0.55是只计算了分母(1-f)+f/k=0.55，忘记求倒数。Amdahl定律的关键：不可优化部分(1-f)是整个系统加速比的天花板。",
    type: "single"
  },
  {
    id: "q-s11-4",
    moduleId: "s11",
    question: "对于按行优先（row-major）存储的 N×N 二维 int 数组 a（int 占 4B，cache 块 64B，N=2048），分析以下两段代码的 cache 行为：\n\n// 代码P：按行遍历\nfor (i = 0; i < N; i++)\n    for (j = 0; j < N; j++)\n        a[i][j] += 1;\n\n// 代码Q：按列遍历\nfor (j = 0; j < N; j++)\n    for (i = 0; i < N; i++)\n        a[i][j] += 1;\n\n以下关于空间局部性（spatial locality）的分析，正确的是？",
    options: [
      { text: "A. 代码P内层循环连续访问同一行的相邻 int（步长 4B），每个 64B cache 块加载后可服务 16 次访问中的 15 次命中，空间局部性好；代码Q内层循环跨行访问（步长 = N×4 = 8192B），每次访问大概率 cache miss", isCorrect: true },
      { text: "B. 代码Q的空间局部性更好，因为外层循环遍历列号 j 是连续的，cache 预取机制能提前加载下一列的所有行数据", isCorrect: false },
      { text: "C. 两段代码的 cache 行为完全相同，因为总访存次数相同（均为 N² 次读写），最终数据吞吐量一致", isCorrect: false },
      { text: "D. 代码P的时间局部性（temporal locality）好于代码Q，因为代码P的内层循环会反复访问同一 cache 块中的元素", isCorrect: false },
    ],
    feedbackCorrect: "正确！代码P按行优先顺序访问：内层循环 j 递增，a[i][j] 和 a[i][j+1] 在内存中相邻（差4B），64B cache块可装16个int——一次miss后后续15次命中，miss rate仅约6.25%。代码Q按列优先访问：内层循环 i 递增而 j 固定，a[i][j] 和 a[i+1][j] 在内存中相差一整行（N×4B=8192B），每次访问都落入不同cache块。当N很大时cache无法同时容纳多行，每次访问几乎必然miss。",
    feedbackWrong: "不对。核心区别在于二维数组的内存布局是行优先的：同一行的元素在物理内存中连续存放（步长=sizeof(int)=4B），不同行同一列的元素相距一整行（步长=N×4B=8192B）。代码P的内层循环沿行方向递增，完美利用空间局部性。代码Q的内层循环沿列方向跨行跳跃，空间局部性极差。选项B的错误在于：外层循环是j连续没错，但决定cache行为的是内层循环的访问模式——内层跨行跳跃导致每次访问大概率miss，硬件预取器对此跨步模式几乎无效。选项C忽略了访问模式对cache命中率的决定性影响。选项D混淆了「空间局部性」和「时间局部性」——代码P的内层循环每次访问不同的a[i][j]，不会被重复使用，时间局部性并无优势。",
    type: "single"
  },
  {
    id: "q-s11-5",
    moduleId: "s11",
    question: "分析以下两段累加代码（n 很大）：\n\n// 原始版本\nfor (i = 0; i < n; i++)\n    for (j = 0; j < n; j++)\n        b[i] += a[i][j] * x[j];\n\n// 优化版本\nfor (i = 0; i < n; i++) {\n    int acc = 0;\n    for (j = 0; j < n; j++)\n        acc += a[i][j] * x[j];\n    b[i] = acc;\n}\n\n优化版本相比原始版本获得性能提升的主要原因是？",
    options: [
      { text: "A. 使用局部变量 acc（通常分配在寄存器中）累加，每行对外层数组 b[i] 只写 1 次；原始版本每次内层迭代都要从内存读 b[i]、浮点加、写回 b[i]，产生约 n 倍的冗余内存读写", isCorrect: true },
      { text: "B. 优化版本减少了乘法运算次数，从原始版本的 n² 次降到 n 次", isCorrect: false },
      { text: "C. 优化版本对数组 x[j] 的访问模式从跨行改为连续，显著提高了 x[j] 的 cache 命中率", isCorrect: false },
      { text: "D. 优化版本使用了循环展开，将内层循环展开为每次处理 4 个元素，减少了循环开销", isCorrect: false },
    ],
    feedbackCorrect: "正确！关键差别在于内存写操作的数量级差异。原始版本中 b[i] 是数组元素（在内存中），每次 += 操作是「读-改-写」三部曲：从内存加载 b[i] → 加上 a[i][j]*x[j] → 写回内存。内层循环执行 n 次，产生 n 次冗余写（只有最后一次写的值会被后续使用）。优化版本用局部变量 acc（编译器分配到寄存器）在寄存器中累加，循环结束后执行一次 b[i]=acc 写内存，将内存写从 O(n²) 降到 O(n)。这种优化称为「寄存器提升」（register promotion）或「标量替换」（scalar replacement）。",
    feedbackWrong: "不对。逐项分析：(1) 两个版本的乘法次数都是 n×n 次，没有减少——选项B错误。(2) 两个版本对 x[j] 的访问模式完全一样（j 从 0 到 n-1 递增），都是连续访问，cache 行为没有差异——选项C错误。(3) 优化版本没有做循环展开，内层循环步长仍为 1——选项D错误。(4) 正确原因是选项A：原始版本每次迭代都做 b[i] 的「读-改-写」，当 n 很大时这 n 次冗余内存操作成为瓶颈；优化版本用寄存器中的局部变量 acc 替代，最终只写一次 b[i]，消除了冗余内存写。这是 CS:APP 第 5.6 节讨论的「减少内存引用」优化——将内存别名提升到寄存器中。",
    type: "single"
  },
  {
    id: "q-s11-6",
    moduleId: "s11",
    question: "对于以下求浮点数组 a[n] 所有元素之和的循环（n 是 4 的倍数），设浮点加法延迟为 3 个时钟周期，每个周期可发射 1 条浮点加法指令：\n\n// 版本X（原始）\nfloat sum = 0;\nfor (i = 0; i < n; i++)\n    sum += a[i];\n\n// 版本Y（优化）\nfloat s0=0, s1=0, s2=0, s3=0;\nfor (i = 0; i < n; i += 4) {\n    s0 += a[i];\n    s1 += a[i+1];\n    s2 += a[i+2];\n    s3 += a[i+3];\n}\nsum = (s0+s1) + (s2+s3);\n\n版本Y相比版本X获得显著加速（可达约 3~4 倍）的最关键原因是？",
    options: [
      { text: "A. 版本Y将 1 条关键路径上的数据依赖链（sum 串行累加，每周期最多 1/3 次加法）拆分为 4 条独立数据流（s0~s3 互不依赖），乱序执行处理器可并行发射并流水化这 4 条链上的浮点加法，将吞吐量提升至接近每周期 1 次", isCorrect: true },
      { text: "B. 版本Y进行了 4 路循环展开，将迭代次数减为 n/4，主要收益来自消除 i 递增和循环条件判断的循环开销", isCorrect: false },
      { text: "C. 版本Y将 4 次连续数组访问合并为一次 burst 读取（a[i]~a[i+3]），减少了地址计算和 TLB 缺失次数", isCorrect: false },
      { text: "D. 版本Y中 s0~s3 类型相同但编译器会分配不同的寄存器 bank，避免了寄存器文件的读写端口冲突", isCorrect: false },
    ],
    feedbackCorrect: "正确！版本X的瓶颈是 sum 的「写后读」（read-after-write）数据依赖：每次 sum += a[i] 必须等待上一次加法的结果写回 sum 寄存器后才能开始。浮点加法延迟 3 周期，因此即使处理器每个周期可发射 1 条加法指令，版本X的吞吐量也被限制在每 3 周期完成 1 次加法——数据依赖链（critical path）成为瓶颈。版本Y使用 4 个独立累加器（s0~s3），它们之间没有数据依赖，处理器的乱序执行引擎可以同时推进这 4 条链：当 s0 的加法在流水线中等待结果时，s1/s2/s3 的加法可以继续发射，将有效吞吐量提升约 4 倍（接近每周期 1 次加法）。这是 CS:APP 第 5.9 节「提高并行性」的核心技巧——多个累加变量（multiple accumulators），也称为「循环展开 + 重新结合」（unrolling + reassociation）。",
    feedbackWrong: "不对。关键区分「循环展开本身」和「多累加器打破数据依赖」。选项B只说了循环展开减少循环开销，但循环开销（i 递增 + 分支判断）在整个执行时间中占比很小，远不足以解释 3~4 倍的加速比。真正的收益来自「打破关键路径」：版本X的 sum 形成串行依赖链（延迟=3周期/次），版本Y的 4 个独立累加器形成 4 条并行依赖链。选项C提到的 burst 读取对性能有帮助但不是主因——两个版本对 a[i] 的访问都是连续流式的，预取器都能处理。选项D的寄存器 bank 冲突在简单循环中不是瓶颈。核心考点：数据依赖限制了指令级并行度（ILP），多累加器是打破这一限制的关键技法。",
    type: "single"
  },
  {
    id: "q-s11-7",
    moduleId: "s11",
    question: "考虑 N×N 矩阵乘法 C = A×B（C 初始化为 0，所有矩阵为 double 型占 8B，行优先存储，cache 块 64B，N 很大）。以下两种循环顺序中，版本2 通常比版本1 快得多：\n\n// 版本1（i-j-k）\nfor (i = 0; i < N; i++)\n    for (j = 0; j < N; j++)\n        for (k = 0; k < N; k++)\n            C[i][j] += A[i][k] * B[k][j];\n\n// 版本2（i-k-j）\nfor (i = 0; i < N; i++)\n    for (k = 0; k < N; k++)\n        for (j = 0; j < N; j++)\n            C[i][j] += A[i][k] * B[k][j];\n\n版本2 性能优越的主要原因是？",
    options: [
      { text: "A. 版本1 内层循环遍历 k：B[k][j] 中 k 是行号，j 固定——每次迭代访问不同行的同一列，跨步 = N×8B，空间局部性极差（几乎每次 cache miss）；版本2 内层循环遍历 j：C[i][j] 和 B[k][j] 均为行内连续访问（步长 = 8B），cache 命中率高", isCorrect: true },
      { text: "B. 版本2 将 C[i][j] 的累加从内层移到中层，使得编译器可以进行自动向量化（SIMD），而版本1 无法向量化", isCorrect: false },
      { text: "C. 版本2 减少了浮点乘法的总次数：版本1 为 N³ 次，版本2 通过复用 A[i][k] 将乘法降为 N² 次", isCorrect: false },
      { text: "D. 版本1 中 C[i][j] 在内层循环被反复读写，产生了「存储-加载转发」（store-to-load forwarding）延迟；版本2 消除了此延迟", isCorrect: false },
    ],
    feedbackCorrect: "正确！核心在于内层循环的访存步长。版本1（i-j-k）的内层 k 循环中：C[i][j] 固定（好），A[i][k] 连续（好），但 B[k][j] 中 k 变化而 j 固定——内存地址 = base + (k×N+j)×8，每次迭代跨过一整行的 N 个元素（N×8B）。当 N 很大时（如 ≥256），每次访问 B[k][j] 都落在不同 cache 行，命中率趋近 0%。版本2（i-k-j）的内层 j 循环中：C[i][j] 和 B[k][j] 均沿行方向连续访问，A[i][k] 在内层固定（被寄存器化，可复用 N 次）——整体空间局部性极佳。这就是「循环交换」（loop interchange）优化：将访问模式差的循环层换到外层，让内层遍历连续维度。",
    feedbackWrong: "不对。逐项分析：(1) 版本1也不影响编译器做自动向量化（只要内层是连续访问的A[i][k]），选项B的理由不准确。(2) 两个版本的浮点乘法次数完全相同（都是N³次）——矩阵乘法的计算量由数学定义决定，循环顺序不影响计算次数，选项C错误。(3) 版本1中C[i][j]在内层固定，只会被store一次（循环结束后），不存在选项D描述的反复读写问题。(4) 正确原因是选项A：关键是B[k][j]的访问模式。版本1中k在内层，B[k][j]跨N行跳跃；版本2中j在内层，B[k][j]行内连续。在行优先存储下，同一行的元素物理相邻，跨行访问意味着跨过N个元素的空间跳跃，命中率天差地别。",
    type: "single"
  },
];

// ── 模拟题 ──
export const S11_SIM_QUESTIONS: ExamQuestion[] = [
  {
    id: "sim-s11-1",
    moduleId: "s11",
    year: "模拟题",
    position: "自编",
    points: 10,
    questionText: "<p><strong>（计算题，10分）</strong>某 Web 服务器处理请求的总执行时间中各阶段占比为：CPU 计算 40%，内存访问 30%，磁盘 I/O 20%，网络通信 10%。现有以下优化方案，请根据 Amdahl 定律分别计算整体的加速比（保留 2 位小数）：</p><p>(1) 仅升级 CPU，使计算速度提升至原来的 4 倍。（2分）</p><p>(2) 仅升级内存，使内存访问速度提升至原来的 3 倍。（2分）</p><p>(3) 仅更换 SSD，使磁盘 I/O 速度提升至原来的 15 倍。（2分）</p><p>(4) 以上三种优化同时实施。（2分）</p><p>(5) 若想使整体加速比达到 3.0 以上，仅靠升级 CPU（CPU 速度趋近无穷大，其他阶段不变）能否实现？请通过计算说明理由。（2分）</p>",
    answerHtml: "<p><strong>通用公式：</strong>S = 1 / [(1 - Σf<sub>优化</sub>) + Σ(f<sub>i</sub> / k<sub>i</sub>)]，其中 f<sub>i</sub> 为第 i 阶段原始时间占比，k<sub>i</sub> 为第 i 阶段的加速倍数。</p><p><strong>(1) 仅升级 CPU（f = 0.4, k = 4）：</strong>S₁ = 1 / [(1-0.4) + 0.4/4] = 1 / [0.6 + 0.1] = 1/0.7 ≈ <strong>1.43</strong>。（2分）</p><p><strong>(2) 仅升级内存（f = 0.3, k = 3）：</strong>S₂ = 1 / [(1-0.3) + 0.3/3] = 1 / [0.7 + 0.1] = 1/0.8 = <strong>1.25</strong>。（2分）</p><p><strong>(3) 仅更换 SSD（f = 0.2, k = 15）：</strong>S₃ = 1 / [(1-0.2) + 0.2/15] = 1 / [0.8 + 0.0133] = 1/0.8133 ≈ <strong>1.23</strong>。（2分）</p><p><strong>(4) 三种优化同时实施：</strong>优化部分的总 f = 0.4 + 0.3 + 0.2 = 0.9，未被优化的网络通信占 0.1。S₄ = 1 / [0.1 + 0.4/4 + 0.3/3 + 0.2/15] = 1 / [0.1 + 0.1 + 0.1 + 0.0133] = 1 / 0.3133 ≈ <strong>3.19</strong>。（2分）</p><p><strong>(5) 仅靠无限升级 CPU：</strong>当 k<sub>CPU</sub> → ∞ 时，f/k → 0。此时 S<sub>max</sub> = 1 / (1-0.4) = 1/0.6 ≈ <strong>1.67</strong> < 3.0。<strong>不能</strong>达到 3.0。（2分）理由：Amdahl 定律的核心约束——不可优化部分（即未被加速的阶段，合计占 60%）成为整体加速比的天花板。无论 CPU 有多快，剩余 60% 的串行/不可加速部分将加速比锁死在 1/0.6 ≈ 1.67 以内。要达到 S≥3.0，必须同时对内存和磁盘 I/O 等瓶颈进行优化——这正是 Amdahl 定律的工程含义：<strong>优化收益递减，瓶颈会转移，必须系统性优化而非单点突破</strong>。</p><p><strong>评分标准：</strong>(1)~(3) 各2分（公式正确1分+结果正确1分）；(4) 2分（正确汇总各项f和k得1分+结果正确1分）；(5) 2分（极限分析正确1分+理由阐述正确1分）。允许计算结果有 ±0.02 的舍入误差。</p>"
  },
  {
    id: "sim-s11-2",
    moduleId: "s11",
    year: "模拟题",
    position: "自编",
    points: 12,
    questionText: "<p><strong>（分析题，12分）</strong>以下代码对一个 N×N 的 float 矩阵 A（行优先存储，float 占 4B）计算每行元素之和，结果存入数组 sum_row[N]。</p><pre>// 原始代码\nfor (i = 0; i < N; i++) {\n    sum_row[i] = 0.0f;\n    for (j = 0; j < N; j++)\n        sum_row[i] += A[i][j];\n}</pre><p>(1) 指出原始代码存在的<strong>性能瓶颈</strong>（至少指出 3 个），并解释每个瓶颈为何影响性能。（6分）</p><p>(2) 针对上述瓶颈，给出<strong>优化版本</strong>的 C 代码（可使用循环展开、多累加器、局部变量等技术），并说明每个优化手段对应解决哪个瓶颈。（4分）</p><p>(3) 设 N = 4096，cache 块 = 64B。原始代码对数组 A 的 cache miss rate 约为多少？优化版本是否改变了这一比例？为什么？（2分）</p>",
    answerHtml: "<p><strong>(1) 性能瓶颈（每个2分，共6分）：</strong></p><p><strong>瓶颈一：冗余内存写（memory alias / store traffic）。</strong>sum_row[i] 是内存中的数组元素，每次 sum_row[i] += A[i][j] 执行的是「从内存加载 sum_row[i] → 浮点加 A[i][j] → 写回 sum_row[i]」。内层循环执行 N 次，产生 N 次冗余的内存写操作——只有最后一次写回的值会被后续代码使用，前 N-1 次写均为浪费。每次内存写涉及 store buffer 占用和可能的 cache 写回/写分配开销。</p><p><strong>瓶颈二：浮点加法数据依赖（data dependency / critical path）。</strong>sum_row[i] 作为标量累加器，每次 + 必须等待上一次加法的结果（read-after-write 依赖）。float 加法典型延迟为 3~4 个时钟周期，即使处理器每个周期可发射 1 条浮点加法指令，实际吞吐量也被限制在每 3~4 周期完成一次加法——关键路径瓶颈使得指令级并行度（ILP）无法发挥。</p><p><strong>瓶颈三：循环开销（loop overhead）。</strong>内层循环每次迭代执行 j 递增 + 与 N 比较 + 条件分支。当 N=4096 时，内层迭代 4096 次，循环开销累计不可忽略（尽管相比瓶颈一和瓶颈二，其影响较小，但在高度优化的代码中仍需消除）。</p><p><strong>(2) 优化版本（4分）：</strong></p><pre>for (i = 0; i < N; i++) {\n    float s0 = 0.0f, s1 = 0.0f, s2 = 0.0f, s3 = 0.0f;  // 局部变量→寄存器\n    int j;\n    for (j = 0; j < N-3; j += 4) {                     // 4路循环展开\n        s0 += A[i][j];\n        s1 += A[i][j+1];\n        s2 += A[i][j+2];\n        s3 += A[i][j+3];\n    }\n    float acc = (s0 + s1) + (s2 + s3);\n    for (; j < N; j++)                                   // 处理剩余元素（tail）\n        acc += A[i][j];\n    sum_row[i] = acc;                                     // 每行只写一次\n}</pre><p><strong>对应关系：</strong>① 局部变量 s0~s3 和 acc 放在寄存器中 → 解决瓶颈一（消除冗余内存写，每行只写一次 sum_row[i]）；② 4 个独立累加器 s0~s3 打破关联 → 解决瓶颈二（4 条独立数据流可并行发射，将浮点加法吞吐量提升约 4 倍）；③ 4 路循环展开（步长 4）→ 解决瓶颈三（循环迭代次数减为约 N/4，减少递增+比较+分支的次数）。单独使用循环展开而不增加累加器个数（如只用1个累加器+4路展开）只能解决瓶颈三，瓶颈二的提升有限——这正是 CS:APP 第 5.9 节强调的「展开 + 多累加器必须配合使用」的原因。</p><p><strong>(3) Cache miss rate 分析（2分）：</strong>A[i][j] 按行优先连续访问，步长 = 4B。64B 的 cache 块可容纳 64/4 = 16 个 float。因此每 16 次访问中，第 1 次为 compulsory miss（冷缺失），后续 15 次命中——miss rate ≈ <strong>1/16 ≈ 6.25%</strong>。优化版本<strong>没有改变</strong>这一比例，因为对 A 的访问模式仍是行内连续访问（j 递增），空间局部性与原始代码完全一致。优化的主要收益来自减少内存写和打破数据依赖链，而非改善对 A 的读取缓存行为。这提示了性能优化的一个重要原则：先解决瓶颈最大的问题（此处是冗余写和依赖链），不必对已经表现良好的部分（A 的行优先读取）做无谓修改。</p><p><strong>评分标准：</strong>(1) 每个瓶颈 2 分（指出瓶颈 1 分 + 解释原因 1 分），至少 3 个共 6 分；(2) 代码正确 2 分 + 对应关系说明 2 分，共 4 分；(3) miss rate 计算正确 1 分 + 是否改变及其理由 1 分，共 2 分。</p>"
  },
  {
    id: "sim-s11-3",
    moduleId: "s11",
    year: "模拟题",
    position: "自编",
    points: 8,
    questionText: "<p><strong>（分析题，8分）</strong>对于 N×N 矩阵乘法 C = A×B（double 型，8B/元素，行优先存储，cache 块 = 64B，N 很大），考虑三种循环顺序：</p><pre>// 顺序I: i-k-j\nfor (i = 0; i < N; i++)\n    for (k = 0; k < N; k++)\n        for (j = 0; j < N; j++)\n            C[i][j] += A[i][k] * B[k][j];\n\n// 顺序II: i-j-k\nfor (i = 0; i < N; i++)\n    for (j = 0; j < N; j++)\n        for (k = 0; k < N; k++)\n            C[i][j] += A[i][k] * B[k][j];\n\n// 顺序III: j-k-i\nfor (j = 0; j < N; j++)\n    for (k = 0; k < N; k++)\n        for (i = 0; i < N; i++)\n            C[i][j] += A[i][k] * B[k][j];</pre><p>(1) 分析<strong>顺序II</strong>（i-j-k）内层循环（k 循环）中 C[i][j]、A[i][k]、B[k][j] 各自的内存访问步长（stride）和空间局部性，判断其 cache 性能优劣。（3分）</p><p>(2) 分析<strong>顺序I</strong>（i-k-j）内层循环（j 循环）中三者的访问步长和局部性，说明为何顺序I通常显著快于顺序II。（3分）</p><p>(3) 顺序III（j-k-i）内层循环（i 循环）的 cache 行为如何？与顺序I 相比孰优孰劣？简要说明理由。（2分）</p>",
    answerHtml: "<p><strong>(1) 顺序II（i-j-k）内层 k 循环分析（3分）：</strong></p><ul><li><strong>C[i][j]：</strong>i 和 j 均固定（来自外层和中层循环），在整个内层 k 循环中不变 → 步长 = 0（固定地址）。时间局部性极好：一次加载后可在寄存器中保持，N 次迭代复用。访问一次，命中 N-1 次（或由编译器放入寄存器，完全消除内存访问）。</li><li><strong>A[i][k]：</strong>i 固定，k 逐次递增 1。地址 = base + (i×N + k)×8。相邻两次访问地址差 = 8B → 步长 = 8B。连续访问，空间局部性好。每个 64B cache 块可装 8 个 double（64/8=8），miss rate ≈ 1/8 = 12.5%。</li><li><strong>B[k][j]：</strong>j 固定，k 逐次递增 1。地址 = base + (k×N + j)×8。相邻两次访问地址差 = N×8B → 步长 = N×8B。跨行跳跃访问，当 N 较大（如 N≥256 时步长≥2048B，远超 64B cache 块）时，每次访问几乎必定 cache miss——空间局部性极差。这是顺序II性能差的主因。</li></ul><p><strong>结论：</strong>顺序II 的瓶颈在于 B[k][j] 的跨行访问模式——内层循环遍历行号 k 而非列号 j，导致 B 矩阵被以列优先方式访问，与行优先存储格式冲突。</p><p><strong>(2) 顺序I（i-k-j）内层 j 循环分析（3分）：</strong></p><ul><li><strong>C[i][j]：</strong>i 固定，j 逐次递增 1。步长 = 8B。连续访问，空间局部性好。与顺序II中 A[i][k] 的行为类似，miss rate ≈ 12.5%。</li><li><strong>B[k][j]：</strong>k 固定（来自中层循环），j 逐次递增 1。步长 = 8B。关键变化：k 被提到中层，j 成为内层——B[k][j] 变为行内连续访问，空间局部性从「极差」变为「好」！miss rate ≈ 12.5%。</li><li><strong>A[i][k]：</strong>i 和 k 均固定（来自外层和中层）。在整个内层 j 循环中不变 → 步长 = 0。一次加载，N 次复用。时间局部性极好（寄存器化）。</li></ul><p><strong>结论：</strong>顺序I 的优越性在于「循环交换」——将原顺序II中跨行访问的 B[k][j] 的 k（行号）换到中层、j（列号）放到内层，使 B 的访问从列优先变为行优先，与内存的行优先存储格式对齐。同时 A[i][k] 被提升为中层固定值，获得 N 次复用的时间局部性优势。</p><p><strong>(3) 顺序III（j-k-i）内层 i 循环分析（2分）：</strong></p><ul><li><strong>C[i][j]：</strong>j 固定，i 逐次递增 1。步长 = N×8B（跨行跳跃）。空间局部性极差。</li><li><strong>A[i][k]：</strong>k 固定，i 逐次递增 1。步长 = N×8B。跨行跳跃，空间局部性极差。</li><li><strong>B[k][j]：</strong>k 和 j 均固定。步长 = 0。固定不变，时间局部性好。</li></ul><p><strong>结论：</strong>顺序III 内层有三个矩阵中的两个（C 和 A）都跨行跳跃访问，每次几乎必定 miss，cache 性能比顺序II 更差。三个顺序的性能排序：<strong>顺序I（ikj）> 顺序II（ijk）> 顺序III（jki）</strong>。核心规律：在行优先存储下，内层循环应遍历连续维度（列号 j），将跨行维度（行号 i 和 k）尽可能放到外层——这就是循环交换优化（loop interchange）的本质。</p><p><strong>评分标准：</strong>(1) 三个矩阵的步长和局部性分析各 1 分，共 3 分；(2) 三个矩阵对比分析各 1 分，共 3 分；(3) 两个矩阵跨行分析 1 分 + 正确排序 1 分，共 2 分。分析中允许用文字描述代替精确数值，但步长大小关系必须正确。</p>"
  },
];
