import type { Quiz } from '@learncourse/framework/types';

export const S8_QUIZZES: Quiz[] = [
  {
    id: "q-s8-16",
    moduleId: "s8",
    question: "HMM 形式化定义 λ = (S, V, A, B, π) 共五个要素。其中 A 和 B 分别是什么？",
    options: [
      { text: "A. A 是转移概率矩阵（状态之间的转移），B 是观测概率矩阵（状态生成观测）", isCorrect: true },
      { text: "B. A 是观测概率矩阵（状态生成观测），B 是转移概率矩阵（状态之间的转移）", isCorrect: false },
      { text: "C. A 是初始状态分布向量，B 是观测符号集合", isCorrect: false },
      { text: "D. A 是动作集合，B 是信念状态分布", isCorrect: false }
    ],
    feedbackCorrect: "正确！A = Transition Matrix（转移矩阵，也称状态转移概率矩阵），描述隐藏状态之间如何跳转：A[i][j] = P(X_{t+1}=j | X_t=i)，每行之和为 1。B = Emission Matrix（发射矩阵/观测概率矩阵/传感模型），描述每个隐藏状态下产生各观测的概率：B[j][k] = P(E_t=k | X_t=j)。记忆技巧：A 像 Action（状态主动变化），B 像 Behavior（状态向外表现）。C 混淆了 π（初始分布向量）和矩阵名称；D 中的「动作」和「信念状态」是 MDP 和 POMDP 的概念，不属于 HMM 五要素。",
    feedbackWrong: "不对。A 是转移矩阵（Transition Matrix），描述隐藏状态之间的跳转概率；B 是发射矩阵（Emission Matrix），描述每个状态产生各观测的概率。C 把 π（初始分布）和 V（观测集合）张冠李戴了。D 引入了 MDP/POMDP 的术语——「动作」和「信念状态」不是 HMM 的组成部分。五要素速记：S（状态集）V（观测集）是集合，A（转移）B（发射）是矩阵，π（初始）是向量——三者的数学类型不同，考试切换选择常利用这一点。"
  },
  {
    id: "q-s8-17",
    moduleId: "s8",
    question: "HMM 三大基本问题中，「给定模型 λ 和观测序列 O，计算该观测序列出现的概率 P(O|λ)」属于哪类问题？其核心算法是？",
    options: [
      { text: "A. 评估问题（Evaluation）—— 前向算法（Forward Algorithm），动态规划递推 α_t(i)", isCorrect: true },
      { text: "B. 解码问题（Decoding）—— 维特比算法（Viterbi Algorithm），求出最可能的隐藏状态路径", isCorrect: false },
      { text: "C. 学习问题（Learning）—— Baum-Welch 算法，从无标注数据中估计 A、B、π", isCorrect: false },
      { text: "D. 评估问题（Evaluation）—— 只能枚举所有 N^T 条路径暴力求和，没有高效算法", isCorrect: false }
    ],
    feedbackCorrect: "正确！评估问题（Evaluation Problem）= 已知模型 λ，求观测序列 O 的概率 P(O|λ)。前向算法用动态规划避免暴力枚举：定义 α_t(i) = P(o_1,...,o_t, X_t=i | λ)，递推 α_{t+1}(j) = [Σ_i α_t(i)·a_{ij}] · b_j(o_{t+1})，最终 P(O|λ) = Σ_i α_T(i)。时间复杂度从 O(T·N^T) 降到 O(T·N²)，N 为状态数。后向算法（Backward）同样能独立完成评估，考试中两种都可能考——区分它们的关键：前向从 t=1 往 t=T 推，后向从 t=T 往 t=1 推。",
    feedbackWrong: "不对。B 描述的是解码问题——目标是「最可能的状态序列」而非「观测序列的总概率」。C 描述的是学习问题——目标是学参数而非算概率。D 更是错的——前向算法（和对称的后向算法）正是为了避免暴力枚举而生的动态规划方法，时间复杂度为 O(T·N²)。三大基本问题对应关系：评估（算概率）= Forward/Backward，解码（找路径）= Viterbi，学习（学参数）= Baum-Welch。这是 HMM 考题的最高频考点之一。"
  },
  {
    id: "q-s8-18",
    moduleId: "s8",
    question: "你有一段连续 10 天的地板干湿观测记录（干/湿序列），想推断每天最可能的天气（晴/雨）序列。应该用 HMM 的哪个算法？",
    options: [
      { text: "A. 前向算法（Forward Algorithm）—— 计算整个干湿序列出现的概率", isCorrect: false },
      { text: "B. 后向算法（Backward Algorithm）—— 配合前向算法做参数估计", isCorrect: false },
      { text: "C. 维特比算法（Viterbi Algorithm）—— 找出最可能产生这个干湿序列的天气路径", isCorrect: true },
      { text: "D. Baum-Welch 算法 —— 从干湿数据中学习转移和发射概率", isCorrect: false }
    ],
    feedbackCorrect: "正确！维特比算法解决「最可能解释」（Most Probable Explanation）问题：给定模型和观测序列，求 argmax_{X_{1:T}} P(X_{1:T} | O_{1:T}, λ)。它用动态规划递推 δ_t(j) = max_i [δ_{t-1}(i) · a_{ij}] · b_j(o_t)，同时记录回溯指针 ψ_t(j)，最后从终点最大值回溯得到完整路径。与滤波的区别：滤波给出每个时刻的边缘分布（可能两状态都有概率），维特比给出一条确定的状态序列。",
    feedbackWrong: "不对。这是解码问题（Decoding），对应的算法是维特比。前向/后向算法只管「这个观测序列出现的总概率是多少」（评估），不关心「具体是哪条路径产生的」（解码）。Baum-Welch 是学参数的——在不知道转移和发射概率时从数据中估计它们。题目场景中模型已知、观测已知，要求反推状态序列，这正是解码 = 维特比的用武之地。三大基本问题的分工：前向=评估，维特比=解码，Baum-Welch=学习——这是 HMM 考题的最高频考点之一。"
  },
  {
    id: "q-s8-19",
    moduleId: "s8",
    question: "关于 HMM 的参数学习算法 Baum-Welch，以下说法正确的是？",
    options: [
      { text: "A. Baum-Welch 是 EM 算法在 HMM 上的特例，可从无标注观测序列中学习模型参数 (A, B, π)", isCorrect: true },
      { text: "B. Baum-Welch 需要人工标注每个时刻的隐藏状态才能训练", isCorrect: false },
      { text: "C. Baum-Welch 保证收敛到全局最优的参数组合", isCorrect: false },
      { text: "D. Baum-Welch 的训练目标是最大化最可能隐藏路径的概率 P(best path | O, λ)", isCorrect: false }
    ],
    feedbackCorrect: "正确！Baum-Welch 是 EM（Expectation-Maximization）算法应用于 HMM 的具体形式。E 步用当前参数通过前向-后向算法计算期望（各时刻处于各状态的期望、各转移发生的期望）；M 步用这些期望统计量更新参数 A、B、π。它只需要观测序列 O，不需要标注隐藏状态——这正是「无监督学习」的含义。如果碰巧有标注数据（知道每个时刻的真实状态），可以直接用极大似然估计（数频次并归一化），比 Baum-Welch 更简单。B 恰好说反了；C 错了——EM 只能保证局部最优；D 混淆了学习目标（max P(O|λ)，似然）与解码目标（max P(X|O,λ)）。",
    feedbackWrong: "不对。B 说反了——Baum-Welch 恰恰不需要标注数据，只有观测序列即可无监督学习（如果有标注，直接用频次统计的极大似然估计更简单）。C 错了——EM 算法只能保证收敛到局部最优，不能保证全局最优，所以实际中常需要多组随机初始化来比较。D 混淆了学习目标与解码目标：学习的目标是最大化观测序列的似然 P(O|λ)（让模型「更能解释」看到的数据），而不是最大化某条特定路径的概率。"
  },
  {
    id: "q-s8-20",
    moduleId: "s8",
    question: "已知前 30 天观测数据 e₁:₃₀。以下三项任务——滤波、预测、平滑——的配对哪一项正确？",
    options: [
      { text: "A. 滤波 —— 求 P(X₂₀ | e₁:₃₀)，其中 20 < 30（回头看过去某一天的状态）", isCorrect: false },
      { text: "B. 预测 —— 求 P(X₂₀ | e₁:₃₀)，其中 20 < 30（用全部证据推断过去）", isCorrect: false },
      { text: "C. 平滑 —— 求 P(X₂₀ | e₁:₃₀)，其中 20 < 30（用全部证据重新估计过去时刻的状态）", isCorrect: true },
      { text: "D. 滤波 —— 求 P(X₃₅ | e₁:₃₀)，其中 35 > 30（向前推未来状态）", isCorrect: false }
    ],
    feedbackCorrect: "正确！滤波/预测/平滑的区分只看一个指标——估计的时刻 k 与当前证据截止时刻 t 的关系：滤波 k = t（实时追踪现在），预测 k > t（向前推未来，纯靠转移矩阵推而不借助新观测），平滑 k < t（回头看过去，因为用了 k 之后的额外证据所以比滤波更准）。A 的 k<t 是平滑不是滤波；B 的 k<t 同样是平滑不是预测；D 的 k>t 是预测不是滤波。记忆口诀：「滤波看现在，预测看未来，平滑回头看」——映射到不等式就是 k=t, k>t, k<t。",
    feedbackWrong: "不对。区分三者的唯一标准是估计时刻 k 与证据截止时刻 t 的大小关系：滤波 k=t（实时追踪现在），预测 k>t（向前推未来，没有新观测校正所以越推越不确定），平滑 k<t（回头看过去，用了未来信息所以更准）。A 中 k=20<t=30 是平滑不是滤波；B 同样是平滑不是预测；D 中 k=35>t=30 是预测不是滤波。这三者在考试中以「配对判断」和「场景识别」两种形式出现——给出公式判断任务类型，或给出业务场景（如「股票实时预估当前趋势」→滤波）判断用哪种推理。"
  }
];

export const S8_SIM_QUESTIONS: ExamQuestion[] = [
  {
    id: "sim-s8-1",
    moduleId: "s8",
    year: "模拟题",
    position: "自编",
    points: 12,
    questionText: "<p><strong>题目：</strong>已知天气 HMM 模型 λ = (S, V, A, B, π)：</p><ul><li>隐藏状态：S = {晴(S₁), 雨(S₂)}，观测：V = {干(O₁), 湿(O₂)}</li><li>初始分布 π：P(X₁=晴)=0.6，P(X₁=雨)=0.4</li><li>转移矩阵 A：P(晴→晴)=0.7，P(晴→雨)=0.3；P(雨→晴)=0.4，P(雨→雨)=0.6</li><li>发射矩阵 B：P(干|晴)=0.9，P(湿|晴)=0.1；P(干|雨)=0.2，P(湿|雨)=0.8</li></ul><p>连续 3 天观测到：<strong>干 → 湿 → 干</strong>。请用维特比算法（Viterbi Algorithm）求出最可能的天气状态序列，并给出该路径的概率值。</p>",
    answerHtml: "<p><strong>维特比算法递推过程：</strong></p><p><strong>t=1（观测=干）：</strong></p><pre>δ₁(晴) = π₁ × P(干|晴) = 0.6 × 0.9 = 0.54\nδ₁(雨) = π₂ × P(干|雨) = 0.4 × 0.2 = 0.08\nψ₁(晴) = 0, ψ₁(雨) = 0（起始无前驱）</pre>\n<p><strong>t=2（观测=湿）：</strong></p><pre>δ₂(晴) = max(δ₁(晴)×A₁₁, δ₁(雨)×A₂₁) × B₁₂\n       = max(0.54×0.7, 0.08×0.4) × 0.1 = max(0.378, 0.032) × 0.1 = 0.0378\nψ₂(晴) = 晴（因为 0.54×0.7 > 0.08×0.4）\n\nδ₂(雨) = max(δ₁(晴)×A₁₂, δ₁(雨)×A₂₂) × B₂₂\n       = max(0.54×0.3, 0.08×0.6) × 0.8 = max(0.162, 0.048) × 0.8 = 0.1296\nψ₂(雨) = 晴（因为 0.54×0.3 > 0.08×0.6）</pre>\n<p><strong>t=3（观测=干）：</strong></p><pre>δ₃(晴) = max(δ₂(晴)×A₁₁, δ₂(雨)×A₂₁) × B₁₁\n       = max(0.0378×0.7, 0.1296×0.4) × 0.9 = max(0.02646, 0.05184) × 0.9 = 0.046656\nψ₃(晴) = 雨（因为 0.1296×0.4 > 0.0378×0.7，注意：雨→晴 的转移更优！）\n\nδ₃(雨) = max(δ₂(晴)×A₁₂, δ₂(雨)×A₂₂) × B₂₁\n       = max(0.0378×0.3, 0.1296×0.6) × 0.2 = max(0.01134, 0.07776) × 0.2 = 0.015552\nψ₃(雨) = 雨（因为 0.1296×0.6 > 0.0378×0.3）</pre>\n<p><strong>回溯（从 t=3 开始）：</strong></p><pre>t=3：max(δ₃(晴), δ₃(雨)) = max(0.046656, 0.015552) = 0.046656 → X₃ = <strong>晴</strong>\nt=2：ψ₃(晴) = 雨 → X₂ = <strong>雨</strong>\nt=1：ψ₂(雨) = 晴 → X₁ = <strong>晴</strong></pre>\n<p><strong>最可能天气序列：晴 → 雨 → 晴，路径概率 = 0.046656。</strong></p><hr /><p><strong>评分标准：</strong></p><ul><li>t=1 的 δ 计算正确（2 分）</li><li>t=2 的 δ 和 ψ 计算正确（3 分）</li><li>t=3 的 δ 和 ψ 计算正确（3 分）</li><li>回溯路径正确（2 分）</li><li>给出最终路径概率值（1 分）</li><li>注意：t=3 处 ψ₃(晴)=雨 而非晴——这一步误判为「晴→晴」是常见错误，会被扣 1 分（1 分）</li></ul>"
  },
  {
    id: "sim-s8-2",
    moduleId: "s8",
    year: "模拟题",
    position: "自编",
    points: 10,
    questionText: "<p><strong>题目：</strong>使用与上题相同的天气 HMM 模型（π、A、B 参数不变），观测到序列 <strong>干 → 湿 → 湿</strong>。请用前向算法（Forward Algorithm）计算 P(O|λ)，即该观测序列出现的概率。</p>",
    answerHtml: "<p><strong>前向算法递推过程：</strong></p><p><strong>初始化 t=1（观测=干）：</strong></p><pre>α₁(晴) = π₁ × P(干|晴) = 0.6 × 0.9 = 0.54\nα₁(雨) = π₂ × P(干|雨) = 0.4 × 0.2 = 0.08</pre>\n<p><strong>递推 t=2（观测=湿）：</strong></p><pre>α₂(晴) = [α₁(晴)×A₁₁ + α₁(雨)×A₂₁] × B₁₂\n       = [0.54×0.7 + 0.08×0.4] × 0.1 = [0.378 + 0.032] × 0.1 = 0.041\n\nα₂(雨) = [α₁(晴)×A₁₂ + α₁(雨)×A₂₂] × B₂₂\n       = [0.54×0.3 + 0.08×0.6] × 0.8 = [0.162 + 0.048] × 0.8 = 0.168</pre>\n<p><strong>递推 t=3（观测=湿）：</strong></p><pre>α₃(晴) = [α₂(晴)×A₁₁ + α₂(雨)×A₂₁] × B₁₂\n       = [0.041×0.7 + 0.168×0.4] × 0.1 = [0.0287 + 0.0672] × 0.1 = 0.00959\n\nα₃(雨) = [α₂(晴)×A₁₂ + α₂(雨)×A₂₂] × B₂₂\n       = [0.041×0.3 + 0.168×0.6] × 0.8 = [0.0123 + 0.1008] × 0.8 = 0.09048</pre>\n<p><strong>终止——求 P(O|λ)：</strong></p><pre>P(O|λ) = α₃(晴) + α₃(雨) = 0.00959 + 0.09048 = <strong>0.10007</strong></pre>\n<p><strong>因此，观测序列「干→湿→湿」出现的概率约为 0.10。</strong></p><hr /><p><strong>评分标准：</strong></p><ul><li>t=1 初始化 α₁ 正确（2 分）</li><li>t=2 递推公式正确、α₂ 值正确（3 分）</li><li>t=3 递推公式正确、α₃ 值正确（3 分）</li><li>终止步求和得到 P(O|λ) 正确（2 分）</li></ul><hr /><p><strong>前向算法 vs 维特比算法的关键区别：</strong></p><ul><li>前向：递推中<strong>求和</strong>（Σ_i），最终输出一个概率标量 P(O|λ)</li><li>维特比：递推中<strong>求最大值</strong>（max_i），最终输出一条状态序列 + 该路径的概率</li><li>前向的「求和」对应边缘化（所有可能路径的概率之和），维特比的「求最大」对应 MAP 估计（最可能的那一条路径）</li></ul>"
  },
  {
    id: "sim-s8-3",
    moduleId: "s8",
    year: "模拟题",
    position: "自编",
    points: 8,
    questionText: "<p><strong>题目：</strong>HMM 之所以能高效计算，依赖于两个核心假设——马尔可夫假设和观测独立假设。请回答以下三个问题：</p><p>(1) 写出马尔可夫假设和观测独立假设的公式表达，并各用一句话解释其含义。（3 分）</p><p>(2) 对于一个两时间步的 HMM，请从全概率公式出发，利用这两个假设，将联合概率 P(X₁, X₂, E₁, E₂) 逐步化简为仅含 π、A、B 元素的乘积形式。（3 分）</p><p>(3) 如果没有这两个假设，直接计算 P(E₁,...,E_T) 需要对多少个可能的隐藏状态序列求和？有了假设后，前向算法的复杂度降为多少？由此说明两个假设的计算意义。（2 分）</p>",
    answerHtml: "<p><strong>(1) 两个假设的公式与含义：</strong></p><p><strong>马尔可夫假设（Markov Assumption）：</strong>P(X_t | X_{1:t-1}) = P(X_t | X_{t-1})。含义：当前隐藏状态仅依赖于上一个隐藏状态，与更早的历史无关——「未来只取决于现在，与过去无关」。</p><p><strong>观测独立假设（Observation Independence Assumption）：</strong>P(E_t | X_{1:t}, E_{1:t-1}) = P(E_t | X_t)。含义：当前观测仅依赖于当前隐藏状态，与历史状态和历史观测均无关——「传感器读数只反映当前的真实状态」。</p><p><strong>(2) 联合概率化简：</strong></p><pre>P(X₁, X₂, E₁, E₂)\n= P(X₁) · P(X₂|X₁) · P(E₁|X₁,X₂,E₁之前) · P(E₂|X₁,X₂,E₁)\n     ↑ 链式法则展开\n= P(X₁) · P(X₂|X₁) · P(E₁|X₁) · P(E₂|X₂)\n     ↑ 两次应用独立假设：P(E₁|...) → P(E₁|X₁)，P(E₂|...) → P(E₂|X₂)\n= π(X₁) · A(X₁→X₂) · B(E₁|X₁) · B(E₂|X₂)\n     ↑ 替换为 HMM 符号</pre>\n<p>最终形式：联合概率 = π × A × B₁ × B₂，即初始分布 × 转移概率 × 各时刻的发射概率之积。</p><p><strong>(3) 计算复杂度对比：</strong></p><p>如果没有两个假设，P(E₁,...,E_T) = Σ_{X_{1:T}} P(X₁,...,X_T, E₁,...,E_T)，需要对所有 N^T 条可能的隐藏状态路径求和——暴力枚举的复杂度为 <strong>O(N^T)</strong>，在状态数 N≥2、时间步 T≥10 时已经不可计算。</p><p>有假设后，前向算法利用动态规划避免重复计算：每个时间步只做 N² 量级的操作，共 T 步，总复杂度为 <strong>O(T · N²)</strong>。这是一个从指数级到多项式级的质变——正是这两个假设让 HMM 从理论上不可计算变得实际可用。</p><hr /><p><strong>评分标准：</strong></p><ul><li>(1) 两个公式各 1 分，各一句话解释 0.5 分（共 3 分）</li><li>(2) 链式法则展开正确 1 分，两次假设应用位置正确各 1 分（共 3 分）</li><li>(3) 暴力复杂度 O(N^T) 1 分，前向复杂度 O(T·N²) 0.5 分，点明「指数→多项式」的质变 0.5 分（共 2 分）</li></ul>"
  }
];